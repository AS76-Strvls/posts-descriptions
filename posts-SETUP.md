# Σημειώσεις & Περιγραφές Δημοσιεύσεων — Οδηγός εγκατάστασης

**Στόχος:** κοινό, ζωντανό «τετράδιο» για τις δημοσιεύσεις του 76ου. Κάθε τμήμα γράφει την **πρώτη ύλη** του κάθε Σάββατο (τι κάναμε / τι μάθαμε) ώστε **να μη χάνεται η πληροφορία μέχρι να γίνει το post** — και ξεχωριστά βλέπουμε τις **τελευταίες δράσεις** («τι παίζει») που τραβιούνται **ζωντανά & δημόσια από την ιστοσελίδα 76.life** (χωρίς σύνδεση/token).

**Αρχιτεκτονική** (ίδιο μοτίβο με erga / μόλα / reimbursement):
**Google Sheet** (πηγή αλήθειας) → **Apps Script Web App** (API read/write) → **`76-posts-descriptions.html`** στο GitHub Pages (η σελίδα που μπαίνει στην ιστοσελίδα).

Αρχεία σε αυτόν τον φάκελο:
- `posts-Code.gs` — ο κώδικας του Apps Script.
- `76-posts-descriptions.html` — η σελίδα για το GitHub Pages / embed.
- `posts-SETUP.md` — αυτός ο οδηγός.

---

## Πώς δουλεύει (2 tabs στη σελίδα)

| Tab | Πηγή | Τι δείχνει |
|---|---|---|
| **✍️ Πρώτη ύλη ανά τμήμα** | Google Sheet «Πρώτη Ύλη» (γράφει/διαβάζει) | Μία γραμμή ανά σημείωση τμήματος: Ημερομηνία · **Έως** · Τμήμα (🟡 Αγέλη / 🟢 Ομάδα / 🔵 Κοινότητα / 💜 Εξτρα) · Τίτλος · Κείμενο · Συντάκτης. |
| **📣 Τελευταίες δράσεις** | **76.life** (WordPress REST, κατηγορία «ΔΡΑΣΕΙΣ») — direct, μόνο ανάγνωση | Οι δημοσιευμένες δράσεις (ημερομηνία + τίτλος + απόσπασμα + σύνδεσμος), με **επιλογέα χρονιάς**. Η σελίδα τις τραβά **απευθείας** από το 76.life (client-side) — **χωρίς σύνδεση/token και χωρίς το Apps Script**. Δουλεύει ακόμη κι αν δεν έχει μπει το `/exec`. |

Άρα το Sheet κρατά **μόνο** την πρώτη ύλη· τα «τελικά posts» ζουν ήδη στην ιστοσελίδα και διαβάζονται από εκεί.

**Ένα app, μία χρονιά τη φορά (φίλτρο, όχι ξεχωριστά αρχεία).** Δεν φτιάχνουμε νέο app/Sheet ανά χρονιά — όλα ζουν σε ένα Sheet και η σελίδα έχει επιλογέα **σκαυτικής χρονιάς (Αύγ–Ιούλ)** στην πρώτη ύλη, προεπιλεγμένα την τρέχουσα. Ένα URL για πάντα, με «Όλες οι χρονιές» για αναζήτηση.

**ΣΚ / κατασκήνωση / εβδομάδα.** Το πεδίο **«Έως»** (προαιρετικό) κάνει μια σημείωση πολυήμερη — π.χ. διήμερο «14–15 Μαρτίου» ή κατασκήνωση «29 Ιουλ – 2 Αυγ». Άφησέ το κενό για μονοήμερο Σάββατο.

Το μοντέλο της πρώτης ύλης είναι **append** (κάθε καταχώρηση = ξεχωριστή γραμμή με μοναδικό ID), οπότε δύο τμήματα που γράφουν ταυτόχρονα **δεν σβήνουν το ένα το άλλο**.

---

## Βήμα 1 — Φτιάξε το Google Sheet
1. Στο Drive του `as@76.life` (π.χ. `My Drive / ΜΟΝΙΜΑ / Δημοσιεύσεις 76`), **New → Google Sheet**. Δώσ' του όνομα (π.χ. `ΔΗΜΟΣΙΕΥΣΕΙΣ_STATE`).
2. Άνοιξέ το και αντίγραψε το **ID** από το URL — το κομμάτι ανάμεσα σε `/d/` και `/edit`.

## Βήμα 2 — Πρόσθεσε το Apps Script
1. Μέσα στο Sheet: **Επεκτάσεις → Apps Script**.
2. Σβήσε ό,τι υπάρχει και **επικόλλησε όλο το `posts-Code.gs`**.
3. Στη γραμμή `const SHEET_ID = 'PASTE_SHEET_ID_HERE';` βάλε το ID από το Βήμα 1. Αποθήκευσε (💾).
4. Στη λίστα συναρτήσεων διάλεξε **`initSheet`** → **Run** μία φορά (Authorize με `as@76.life`). Δημιουργείται το φύλλο «Πρώτη Ύλη» με τις κεφαλίδες. Τρέξ' το μόνο την πρώτη φορά.

> Το Apps Script αφορά **μόνο** την «Πρώτη ύλη» (Sheet). Οι «Τελευταίες δράσεις» τραβιούνται απευθείας από το 76.life μέσα από το HTML (`SITE_URL`, `DRASEIS_CAT=29` στη σελίδα) — δεν χρειάζονται deploy/URL.

## Βήμα 4 — Δημοσίευσε το Web App
1. **Deploy → New deployment** → τύπος **Web app**.
2. *Execute as*: **Me (as@76.life)** · *Who has access*: **Anyone**.
3. **Deploy** → αντίγραψε το **Web app URL** (τελειώνει σε `/exec`).
> Σε κάθε μελλοντική αλλαγή του `posts-Code.gs`: **Deploy → Manage deployments → Edit → New version** (αλλιώς δεν «περνάει» η αλλαγή).

## Βήμα 5 — Βάλε το URL στη σελίδα
Στο `76-posts-descriptions.html`, κοντά στην αρχή του `<script>`:
```js
const WEB_APP_URL = "PASTE_WEB_APP_URL_HERE";
```
Βάλε εκεί το `/exec` URL. Είναι η **μόνη** γραμμή που αλλάζεις.

## Βήμα 6 — Ανέβασέ το στο GitHub Pages
1. Ανέβασε το **`76-posts-descriptions.html`** στο repo (κράτα το μοναδικό όνομα — μην το κάνεις `index.html`, για να μη συγκρούεται με τις άλλες σελίδες).
2. **Settings → Pages → Build from branch → main / root**.
3. Η σελίδα: `https://<user>.github.io/<repo>/76-posts-descriptions.html`. Μοίρασέ την στους βαθμοφόρους / βάλ' την (iframe) στην ιστοσελίδα.

---

## Τελευταίες δράσεις — γιατί ΔΕΝ χρειάζεται «connect»
Το tab «📣 Τελευταίες δράσεις» διαβάζει **απευθείας** το δημόσιο WordPress REST API του 76.life (`/wp-json/wp/v2/posts?categories=29&after=…&before=…`), **client-side μέσα από τον browser**. Το 76.life επιστρέφει CORS headers, οπότε δουλεύει κατευθείαν — **καμία σύνδεση, κανένα token, κανένα Apps Script**. Ο επιλογέας χρονιάς φιλτράρει με σκαυτικά όρια (1 Αυγ – 1 Αυγ). Η ημερομηνία διαβάζεται από τον τίτλο της δράσης (μορφή «DD/MM …»), με fallback την ημ. δημοσίευσης.

> **Instagram;** Δεν χρειάζεται. Παρόλο που τα posts είναι «δημόσια», το Instagram/Meta **δεν** αφήνει να τα διαβάσεις χωρίς token (θέλει app + Business account). Η ιστοσελίδα αντίθετα είναι ανοιχτή για ανάγνωση — οπότε την ιστοσελίδα χρησιμοποιούμε.

## Καθημερινή χρήση
- **Τμήματα:** ανοίγουν τη σελίδα → tab «✍️ Πρώτη ύλη ανά τμήμα» → διαλέγουν τμήμα (chip) → βλέπουν όλες τις παλιές τους σημειώσεις ανά Σάββατο → προσθέτουν τη σημείωση της εβδομάδας (κουμπιά «Αυτό/Προηγ. Σάββατο», πεδίο «Έως» για ΣΚ/κατασκήνωση).
- **Όλοι:** tab «📣 Τελευταίες δράσεις» → βλέπουν αυτόματα τι έχει δημοσιευτεί στην ιστοσελίδα, με σύνδεσμο «άνοιγμα ↗». Κουμπί «↻ Ανανέωση».
- Ο πίνακας πάνω δεξιά δείχνει «Συνδεδεμένο» όταν μιλά με το Sheet. Το Sheet κρατά **Ιστορικό εκδόσεων** (Αρχείο → Ιστορικό) — αναιρείς λάθη.

## GitHub (έγινε)
- Repo: **`AS76-Strvls/posts-descriptions`** (Public) · GitHub Pages ενεργό (main / root).
- Live σελίδα: **https://as76-strvls.github.io/posts-descriptions/76-posts-descriptions.html** (χτίζεται ~1 λεπτό μετά το push).

## Εκκρεμούν (Andreas)
- [ ] Φτιάξε το Sheet + πάρε το **SHEET_ID** → πέρασέ το στο `posts-Code.gs`.
- [ ] Run `initSheet` (μία φορά) + **Deploy** → πάρε το **/exec URL**.
- [ ] Βάλε το `/exec` στο `WEB_APP_URL` του HTML → **ξανα-ανέβασε** το αρχείο στο repo (ή edit απευθείας στο GitHub το ένα line). Μέχρι τότε η σελίδα φορτώνει αλλά δείχνει «Χωρίς σύνδεση».
- [ ] embed στην ιστοσελίδα (iframe).
- [ ] (προαιρετικά) Στήσε τα αρχικά «νήματα» — π.χ. αντέγραψε τις σημειώσεις Ομάδας από το doc `Ομάδα Προσκόπων — Σημειώσεις Δράσεων`.

## Σημειώσεις
- **Ασφάλεια:** «Anyone» = όποιος έχει το URL γράφει. Πιο κλειστό: *Anyone with Google account* (login).
- **GDPR / λεκτικά συναίνεσης** (αν μπει σε δημόσια ιστοσελίδα με ονόματα βαθμοφόρων) → **CDPS - Consulting** πριν το go-live.
- Το «Συντάκτης» είναι ελεύθερο πεδίο (όχι μητρώο) — μην βάζετε ονόματα ανηλίκων εκεί.
